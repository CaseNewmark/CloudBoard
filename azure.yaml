# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: cloud-board
services:  
  app:
    language: dotnet
    project: ./CloudBoard.AppHost/CloudBoard.AppHost.csproj
    host: containerapp
